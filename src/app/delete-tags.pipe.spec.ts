import {DeleteTagsPipe} from './delete-tags.pipe';

describe('DeleteTagsPipe', () => {
    it('create an instance', () => {
        const pipe = new DeleteTagsPipe();
        expect(pipe).toBeTruthy();
    });
});
