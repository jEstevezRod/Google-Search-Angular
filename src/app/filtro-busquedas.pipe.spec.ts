import {FiltroBusquedasPipe} from './filtro-busquedas.pipe';

describe('FiltroBusquedasPipe', () => {
    it('create an instance', () => {
        const pipe = new FiltroBusquedasPipe();
        expect(pipe).toBeTruthy();
    });
});
